name: nfc_reader
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  nfc_manager: ^3.5.0
  intl: ^0.20.2

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true